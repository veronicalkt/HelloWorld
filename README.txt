1) To run this Hello, World! application, install the dependencies with npm install,
cd into hello-world-ts, and then run npm run dev

2) I used the code from the bootcamp and the terminal command
npx create-react-app hello-world-ts --template typescript
to create a template react app that I modified to display the text "Hello, World!"
